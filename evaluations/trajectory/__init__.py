# Trajectory Evaluation — scores the agent's execution path, not just the output
#
# Uses the EventBus trace to evaluate planning, tool selection, error recovery,
# and execution efficiency. Complements existing output-only evaluations.
